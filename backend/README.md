# Backend

The backend is the local detector service started by `../run.sh`.

It exposes the unauthenticated face detection endpoint:

```text
POST /find_faces
```

The admin service, API service, Postgres service, login flow, and user-management files are intentionally not included.
