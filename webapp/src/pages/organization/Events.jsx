import { useState } from "react";
import Badge from "../../components/Badge";
import Icon from "../../components/Icon";
import Modal from "../../components/Modal";
import { usePlatform } from "../../context/PlatformContext";
import "../college/Events.css";

const today = "2026-08-12";
export default function Events() {
  const { events, addEvent } = usePlatform();
  const [open, setOpen] = useState(false);
  const [form, setForm] = useState({ name:"", description:"", date:today, location:"", retentionDays:90, expiresAt:"2026-11-10" });
  function submit(event) { event.preventDefault(); addEvent(form); setOpen(false); setForm({ name:"", description:"", date:today, location:"", retentionDays:90, expiresAt:"2026-11-10" }); }
  return <div className="page"><div className="page-head"><div><p className="eyebrow">Event workspace</p><h2>Events</h2><p>Create an event, enroll participants, process photos and deliver private galleries.</p></div><button className="btn primary" onClick={() => setOpen(true)}><Icon name="plus" size={16}/> Create event</button></div><div className="workflow-strip">{["Create event","Participants","Event photos","Face processing","Private galleries"].map((step,index) => <div key={step}><span>{index+1}</span><p>{step}</p>{index<4?<Icon name="arrow" size={15}/>:null}</div>)}</div><div className="events-grid">{events.map((event) => { const progress = event.status === "delivered" ? 100 : event.status === "ready" ? 92 : event.status === "processing" ? 46 : 6; return <article className="card event-card modern" key={event.id}><div className="event-card-head"><div className="date-tile"><strong>{new Date(`${event.date}T00:00:00`).getDate()}</strong><span>{new Date(`${event.date}T00:00:00`).toLocaleString("en",{month:"short"})}</span></div><Badge status={event.status}/></div><div><h3>{event.name}</h3><p className="event-date">{event.location}</p></div><div className="event-progress"><div><span>Workflow progress</span><strong>{progress}%</strong></div><div className="progress-track"><span style={{width:`${progress}%`}}/></div></div><div className="event-stats"><div><p className="detail-stat-label">Photos</p><p className="detail-stat-value">{event.photos.toLocaleString()}</p></div><div><p className="detail-stat-label">Participants</p><p className="detail-stat-value">{event.participants}</p></div><div><p className="detail-stat-label">Matched</p><p className="detail-stat-value">{event.matched}</p></div></div><footer><span>Expires {event.expiresAt}</span><button className="btn ghost small">Open event <Icon name="arrow" size={14}/></button></footer></article>})}</div><Modal open={open} onClose={() => setOpen(false)} title="Create event" description="An event ID and isolated storage path will be created automatically." footer={<><button className="btn" onClick={() => setOpen(false)}>Cancel</button><button className="btn primary" form="event-form">Create event</button></>}><form id="event-form" className="form-grid" onSubmit={submit}><div className="field full"><label>Event name</label><input required value={form.name} onChange={(e)=>setForm({...form,name:e.target.value})} placeholder="e.g. GDG DevFest 2026"/></div><div className="field full"><label>Description</label><textarea rows="3" value={form.description} onChange={(e)=>setForm({...form,description:e.target.value})}/></div><div className="field"><label>Date</label><input type="date" required value={form.date} onChange={(e)=>setForm({...form,date:e.target.value})}/></div><div className="field"><label>Location</label><input required value={form.location} onChange={(e)=>setForm({...form,location:e.target.value})}/></div><div className="field"><label>Retention period</label><select value={form.retentionDays} onChange={(e)=>setForm({...form,retentionDays:Number(e.target.value)})}><option value="30">30 days</option><option value="60">60 days</option><option value="90">90 days</option><option value="180">180 days</option></select></div><div className="field"><label>Data expiry</label><input type="date" value={form.expiresAt} onChange={(e)=>setForm({...form,expiresAt:e.target.value})}/></div></form></Modal></div>;
}
